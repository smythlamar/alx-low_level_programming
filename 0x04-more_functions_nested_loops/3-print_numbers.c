#include "main.h"
/**
 * print_numbers - func checks for a digit (0 through)
 *
 * Return: Always 0
 */
void print_numbers(void)
{
	int c;

	for (c = 48; c < 58; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
